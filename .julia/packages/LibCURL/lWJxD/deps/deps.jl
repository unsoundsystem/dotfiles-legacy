## This file autogenerated by BinaryProvider.write_deps_file().
## Do not edit.
##
## Include this file within your main top-level source, and call
## `check_deps()` from within your module's `__init__()` method

if isdefined((@static VERSION < v"0.7.0-DEV.484" ? current_module() : @__MODULE__), :Compat)
    import Compat.Libdl
elseif VERSION >= v"0.7.0-DEV.3382"
    import Libdl
end
const libz = joinpath(dirname(@__FILE__), "usr/lib/libz.so")
const libmbedx509 = joinpath(dirname(@__FILE__), "usr/lib/libmbedx509.so")
const libmbedcrypto = joinpath(dirname(@__FILE__), "usr/lib/libmbedcrypto.so")
const libmbedtls = joinpath(dirname(@__FILE__), "usr/lib/libmbedtls.so")
const libcurl = joinpath(dirname(@__FILE__), "usr/lib/libcurl.so")
function check_deps()
    global libz
    if !isfile(libz)
        error("$(libz) does not exist, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libz) in (C_NULL, nothing)
        error("$(libz) cannot be opened, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    global libmbedx509
    if !isfile(libmbedx509)
        error("$(libmbedx509) does not exist, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libmbedx509) in (C_NULL, nothing)
        error("$(libmbedx509) cannot be opened, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    global libmbedcrypto
    if !isfile(libmbedcrypto)
        error("$(libmbedcrypto) does not exist, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libmbedcrypto) in (C_NULL, nothing)
        error("$(libmbedcrypto) cannot be opened, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    global libmbedtls
    if !isfile(libmbedtls)
        error("$(libmbedtls) does not exist, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libmbedtls) in (C_NULL, nothing)
        error("$(libmbedtls) cannot be opened, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    global libcurl
    if !isfile(libcurl)
        error("$(libcurl) does not exist, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libcurl) in (C_NULL, nothing)
        error("$(libcurl) cannot be opened, Please re-run Pkg.build(\"LibCURL\"), and restart Julia.")
    end

end
